package usecase.draft_words;

/**
 * Interface for draft words input boundary.
 */
public interface DraftWordsInputBoundary {
    /**
     * Execute method for draft words input boundary.
     * @param draftWordsInputData Input data to draft words interactor.
     */
    void execute(DraftWordsInputData draftWordsInputData);
}
